[package]
name = "apex-evidence"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Revision-bound evidence and completion gates for ApexCode"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
apex-runtime-trust = { path = "../apex-runtime-trust" }
